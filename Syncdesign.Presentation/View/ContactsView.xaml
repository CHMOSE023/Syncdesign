<UserControl x:Class="Syncdesign.Presentation.View.ContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Syncdesign.Presentation.View"
             xmlns:vm="clr-namespace:Syncdesign.Presentation.ViewModel"
             xmlns:bim="http://bimhub.top/syncdesign/2026/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:beh="clr-namespace:Syncdesign.Presentation.Behavior"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <bim:ControlsDictionary/>
                <bim:ThemesDictionary/>
            </ResourceDictionary.MergedDictionaries>             
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid ClipToBounds="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel  Grid.Row="0"  Background="Transparent">
            <Border BorderBrush="LightGray" BorderThickness=" 0 0 0 1" >
                <i:Interaction.Behaviors>
                    <beh:ScrollHeaderHideBehavior ScrollSpeed="30"/>
                </i:Interaction.Behaviors>
                <StackPanel  Background="Transparent">
                    <bim:Button Style="{StaticResource SyncdesignUiButton}"                  
                                    Margin="10 6 10 6"  
                                    Height="32"
                                    Background="#F5f5f5" 
                                    HoverBackground="#F2f2f2"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    BorderBrush="LightGray">

                        <StackPanel   Orientation="Horizontal"   HorizontalAlignment="Center" >
                            <bim:SymbolIcon FontSize="18"  Symbol="PersonSettings20"/>
                            <Label Content="好友管理器" Foreground="{Binding Foreground,  RelativeSource={RelativeSource AncestorType=bim:Button}}"/>
                        </StackPanel>
                    </bim:Button>
                    <bim:Button  Style="{StaticResource SyncdesignUiButton}"
                                     Height="32"
                                     Background="Transparent" 
                                     HoverBackground="#F2f2f2"
                                     CornerRadius="0"
                                     BorderThickness="0"
                                     BorderBrush="LightGray">
                        <Grid  Margin="10 0 10 0" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label  Content="好友通知"   
                                        VerticalAlignment="Center"   
                                        Foreground="{Binding Foreground,   RelativeSource={RelativeSource AncestorType=bim:Button}}"/>

                            <bim:SymbolIcon Grid.Column="1"
                                        FontSize="18"
                                        Symbol="ChevronRight20"
                                        VerticalAlignment="Center"/>
                        </Grid>
                    </bim:Button>
                    <bim:Button  Style="{StaticResource SyncdesignUiButton}"                   
                                     Height="32"
                                     CornerRadius="0"
                                     Background="Transparent" 
                                     HoverBackground="#F2f2f2"
                                     BorderThickness="0"
                                     BorderBrush="LightGray">

                        <Grid   Margin="10 0 10 0" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label Content="项目通知"                     
                                       VerticalAlignment="Center"   
                                       Foreground="{Binding Foreground,   RelativeSource={RelativeSource AncestorType=bim:Button}}"/>

                            <bim:SymbolIcon Grid.Column="1"
                                                FontSize="18"
                                                Symbol="ChevronRight20"
                                                VerticalAlignment="Center"/>
                        </Grid>
                    </bim:Button>
                </StackPanel>
            </Border>

            <bim:Segmented Grid.Row="0"
                                 Margin ="9 6 9 6"
                                 ItemsSource="{Binding Tabs}" 
                                 SelectedItem="{Binding CurrentTab,Mode=TwoWay}"                           
                                 />

        </StackPanel>
        
        <TreeView Grid.Row="1"
                  ItemsSource="{Binding ContactGroups}"
                  BorderThickness="0"
                  Background="Transparent">
            <i:Interaction.Behaviors>
                <beh:TreeViewSelectedItemBehavior SelectedItem="{Binding CurrentSelectedObject, Mode=TwoWay}"/>
            </i:Interaction.Behaviors>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TreeViewItem">
                                <StackPanel>
                                    <ContentPresenter x:Name="PART_Header" ContentSource="Header" />
                                    <ItemsPresenter x:Name="ItemsHost"  Visibility="Collapsed"/>
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded" Value="True">
                                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.ItemContainerStyle>
            
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type vm:ContactGroup}" 
                                          ItemsSource="{Binding Items}">
                    <bim:SectionExpander IsExpanded="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=TreeViewItem}}">
                        <bim:SectionExpander.Header>
                            <Grid Margin="0 0 10 0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding GroupName}" VerticalAlignment="Center" Foreground="#333"/>
                                <TextBlock Grid.Column="1" Text="{Binding Items.Count, StringFormat='({0})'}" 
                                           Margin="3,0,0,0" VerticalAlignment="Center" FontSize="11" Foreground="#999"/>
                            </Grid>
                        </bim:SectionExpander.Header>
                    </bim:SectionExpander>
                </HierarchicalDataTemplate>

                <DataTemplate DataType="{x:Type vm:Contact}">
                    <Border x:Name="Root" Background="Transparent">
                        <Grid Margin="12 6 6 6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Width="40" Height="40" CornerRadius="20">
                                <Image Source="{Binding Icon}" Stretch="UniformToFill"   RenderOptions.BitmapScalingMode="HighQuality">
                                    <Image.Clip>
                                        <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                                    </Image.Clip>
                                </Image>
                            </Border> 
                            
                            <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold" Foreground="#222"/>
                                <TextBlock Text="在线" FontSize="12" Margin="0 3 0 0" Foreground="#666"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <DataTemplate.Triggers>
                        <!--没有选中时候鼠标进入-->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="Root" Property="Background" Value="#F2F2F2"/>
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#DDD"/>
                        </DataTrigger>
                  
                        
                    </DataTemplate.Triggers>
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>
      
    </Grid>

</UserControl>
