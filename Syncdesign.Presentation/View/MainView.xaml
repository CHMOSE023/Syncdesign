<UserControl x:Class="Syncdesign.Presentation.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Syncdesign.Presentation.View" 
             xmlns:vm="clr-namespace:Syncdesign.Presentation.ViewModel"  
             xmlns:bim="http://bimhub.top/syncdesign/2026/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="450">    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <bim:ControlsDictionary/>
                <bim:ThemesDictionary  />                
            </ResourceDictionary.MergedDictionaries>                
        </ResourceDictionary>
    </UserControl.Resources>     
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0"  Content="{Binding HeaderVM}" />
        <!--<local:Header />-->
        <Border Grid.Row="1" Margin="3" CornerRadius="6" Background="White">
            <Grid >
                <Grid.ColumnDefinitions >
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/> 
                </Grid.ColumnDefinitions> 
                <Border Grid.Column="0"  BorderThickness="0,0,1,0" BorderBrush="LightGray">
                    <!--绑定到SidebarViewModel实例中-->
                    <ContentControl Content="{Binding SidebarVM}" />
                </Border>
                <!--动态内容-->
                <ContentControl Grid.Column="1" Content="{Binding ContainerVM}" /> 
            </Grid>
        </Border>
    </Grid>
</UserControl>
